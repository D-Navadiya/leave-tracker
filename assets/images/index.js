const Logo = require('./logo.png');
const LogoWithText = require('./logo_with_text.png');
const TextLogo = require('./text_logo.png');

const images = {
  Logo,
  LogoWithText,
  TextLogo,
};

export default images;
